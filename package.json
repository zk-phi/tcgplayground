{
  "name": "dm-playground",
  "private": true,
  "scripts": {
    "dev": "concurrently 'csso --watch -i ./src/styles.css -o ./src/styles.min.css' 'vite'",
    "build": "csso -i ./src/styles.css -o ./src/styles.min.css && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@preact/signals": "^2.0.1",
    "concurrently": "^9.1.2",
    "csso-cli": "^4.0.2",
    "preact": "^10.25.4",
    "vite": "^6.0.7"
  }
}
